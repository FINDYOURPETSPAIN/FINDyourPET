<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="findyourpet">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.

         ESCRIBIR AQUI LOS PERMISOS NECESARIOS
    -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Maps -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--
 uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!- Poner aquí los permisos que necesita la aplicación,
    -->
    <!-- automáticamente si el usuario los acepta se cogen -->
    <!-- necesitamos: -->
    <!-- + BROADCAST_SMS: "Android.permission.BROADCAST_SMS" Permite que una aplicación emita una notificación de recepción de SMS. -->
    <!-- + READ_SMS: "android.permission.READ_SMS" Permite que una aplicación lea los mensajes SMS. -->
    <!-- + RECEIVE_SMS:  "android.permission.RECEIVE_SMS" Permite que una aplicación reciba mensajes SMS. -->
    <!-- + SEND_SMS: "android.permission.SEND_SMS" Permite que una aplicación envíe mensajes SMS. -->
    <!-- + ANSWER_PHONE_CALLS: "android.permission.ANSWER_PHONE_CALLS" Permite que la aplicación conteste una llamada entrante. -->
    <!-- + CALL_PHONE:  "android.permission.CALL_PHONE" Permite que una aplicación inicie una llamada telefónica sin pasar por la interfaz de usuario del Marcador para que el usuario confirme la llamada. -->
    <!-- + CALL_PRIVILEGED:  "android.permission.CALL_PRIVILEGED" Permite que una aplicación llame a cualquier número de teléfono, incluidos los números de emergencia, sin pasar por la interfaz de usuario del Marcador para que el usuario confirme que se ha realizado la llamada. -->
    <!-- + MANAGE_OWN_CALLS: "android.permission.MANAGE_OWN_CALLS" Permite una aplicación de llamada que gestiona sus propias llamadas a través de las ConnectionServiceAPI autogestionadas . Consulte {@link android.telecom.PhoneAccount # CAPABILITY_SELF_MANAGED para obtener más información sobre las API de ConnectionService autogestionadas. -->
    <!-- + MODIFY_PHONE_STATE: "android.permission.MODIFY_PHONE_STATE" Permite la modificación del estado de la telefonía: encendido, mmi, etc. No incluye realizar llamadas. -->
    <!-- + PROCESS_OUTGOING_CALLS: "android.permission.PROCESS_OUTGOING_CALLS" Permite que una aplicación vea el número que se marca durante una llamada saliente con la opción de redirigir la llamada a un número diferente o abortar la llamada por completo. -->
    <!-- + READ_CALL_LOG: "android.permission.READ_CALL_LOG" Permite que una aplicación lea el registro de llamadas del usuario. -->
    <!-- + READ_PHONE_STATE: "android.permission.READ_PHONE_STATE" Permite el acceso de solo lectura al estado del teléfono, incluido el número de teléfono del dispositivo, la información actual de la red celular, el estado de las llamadas en curso y una lista de todas PhoneAccountlas registradas en el dispositivo. -->
    <!-- + ACCESS_COARSE_LOCATION:  "android.permission.ACCESS_COARSE_LOCATION" Permite que una aplicación acceda a la ubicación aproximada. Alternativamente, puede que quieras ACCESS_FINE_LOCATION. -->
    <!-- + ACCESS_LOCATION_EXTRA_COMMANDS: "android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" Permite que una aplicación acceda a comandos de proveedor de ubicación adicionales. -->
    <!-- + INSTALL_LOCATION_PROVIDER:  "android.permission.INSTALL_LOCATION_PROVIDER" Permite que una aplicación instale un proveedor de ubicación en el Administrador de ubicación. -->
    <!-- + LOCATION_HARDWARE:  "android.permission.LOCATION_HARDWARE" Permite que una aplicación use características de ubicación en el hardware, como la API de geocerca. -->
    <!-- + READ_CONTACTS: "android.permission.READ_CONTACTS" Permite que una aplicación lea los datos de los contactos del usuario. -->
    <!-- + READ_PHONE_NUMBERS: "android.permission.READ_PHONE_NUMBERS" Permite el acceso de lectura al número (s) de teléfono del dispositivo. Este es un subconjunto de las capacidades otorgadas READ_PHONE_STATEpero está expuesto a aplicaciones efímeras. -->
    <!-- + CAMERA:  "android.permission.CAMERA" Necesario para poder acceder al dispositivo de la cámara. -->
    <!-- + ACCESS_NOTIFICATION_POLICY: "android.permission.ACCESS_NOTIFICATION_POLICY" Permiso de marcador para las aplicaciones que desean acceder a la política de notificaciones. -->
    <!-- + ACCESS_WIFI_STATE: "android.permission.ACCESS_WIFI_STATE" Permite que las aplicaciones accedan a información sobre redes Wi-Fi. -->
    <!-- + VIBRATE: "android.permission.VIBRATE" Permite el acceso al vibrador. -->
    <!-- + REBOOT:  "android.permission.REBOOT" Necesario para poder reiniciar el dispositivo. -->
    <!-- <permission android:name="com.example.project.DEBIT_ACCT"/> -->
    <!-- <uses-permission android:name="com.example.project.DEBIT_ACCT" /> -->
    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/logo_fyp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_fyp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="HardcodedDebugMode">
        <activity
            android:name=".vistas.Main"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vistas.Login"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".vistas.PreRegistro"
            android:label="PreRegistro" />
        <activity
            android:name=".vistas.Registro"
            android:label="@string/title_activity_registro" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".vistas.SmsManage"
            android:label="GestionSMS" />
        <activity
            android:name=".vistas.Registro_dispositivo"
            android:label="RegistroDeDispositivo" />
        <activity
            android:name=".vistas.List_Localize"
            android:label="ListadoDispositivosLocalizar" />
        <activity
            android:name=".vistas.List_Gestion"
            android:label="ListadoDispositivosGestionar" />
        <activity
            android:name=".vistas.Password"
            android:label="RecordarPassword" />
        <activity
            android:name=".vistas.CallView"
            android:label="Llamar a dispositivo" />
        <activity
            android:name=".vistas.SmsIncoming"
            android:label="leer sms a dispositivo" />
        <activity
            android:name=".vistas.SmsIsIn"
            android:label="ver sms en dispositivo" />
        <activity
            android:name=".vistas.Responder_preguntas"
            android:label="responder pregunta seguridad" />
        <activity
            android:name=".vistas.NuevaPass"
            android:label="Renovar password usuario" />
        <activity
            android:name=".vistas.Home"
            android:label="@string/title_activity_home_front"
            android:theme="@style/AppTheme" />
        <activity
            android:name="comwelcome.wixsite.findyourpetspain.findyourpet.MapLocalize"
            android:label="@string/title_activity_map_localize"></activity>
    </application>

</manifest>